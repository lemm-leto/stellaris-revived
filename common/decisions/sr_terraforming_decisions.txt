srt_show_status = {
	sound = event_administrative_work # ToDo
	icon = decision_srt_show_status

	resources = {
		category = decisions
	}

	potential = {
		exists = space_owner
    space_owner = { is_same_value = from }
    NOT = { from = { has_country_flag = sr_terraforming_window_open} }
    OR = {
      is_planet_class = pc_barren
      is_planet_class = pc_barren_cold
      is_planet_class = pc_frozen
      is_planet_class = pc_molten
      is_planet_class = pc_toxic
    }
	}

	allow = {
		from = {
			NOT = { has_country_flag = sr_terraforming_window_open }
		}
	}

	effect = {
		custom_tooltip = "srt_show_status_window_open"
		hidden_effect = {
      from = {
        set_country_flag = sr_terraforming_window_open
			}
			save_global_event_target_as = terraforming_planet
			from = {
				country_event = { id = sr_terraforming.1000 }
			}
    }
	}

	ai_weight = { weight = 0 } # ToDo - add support of AI usage
}